server:
  port: 9090

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/proyecto_final
    username: user
    password: password123
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true

